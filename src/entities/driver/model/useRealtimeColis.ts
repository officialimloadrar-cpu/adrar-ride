import { useEffect,useState } from "react";import { supabase } from "@/shared/lib/api/supabase";export const useRealtimeColis=()=>{const [data,setData]=useState<any[]>([]);useEffect(()=>{if(!supabase) return; const ch=supabase.channel("colis").on("postgres_changes",{event:"*",schema:"public",table:"orders"},(p:any)=>setData((x:any[])=>[...x,(p.new as any)])).subscribe();return()=>{supabase.removeChannel(ch);};},[]);return data;};